{:app {:env "production"}
 :server {:port 3000
          :logging? true
          :join? true}
 :test {:strategy :container-dockerfile
        :wait-for {:strategy :health}
        :db-name "ehr"
        :docker-file "test-db/Dockerfile"
        :env-vars {"POSTGRES_PASSWORD" "postgres"
                   "POSTGRES_USER" "postgres"}
        :exposed-ports [5432]}
 :db {:host "localhost"
      :port 5432
      :username "postgres"
      :database "ehr"}}
